import pytest

import odd_and_even_sum1


def test_odd_and_even_number(self, num):
    number = list(range(1, num))
    assert odd_and_even_sum1 == 20, 25

    # self.assertEqual(expected, odd_and_even_sum1.test_odd_and_even_number(num))
